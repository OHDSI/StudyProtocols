To run this make sure shiny and DT are installed:
install.packages(shiny)
install.packages(DT)

then inside R run:
shiny::runGitHub(repo='OHDSI/StudyProtocols',subdir='CelecoxibPredictiveModelsUI', ref='new_plp')
